name: test

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Check git tags
        run: echo "GIT_TAG=`echo $(git describe --tags --abbrev=0)`"
